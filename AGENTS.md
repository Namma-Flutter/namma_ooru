Flutter FVM: use `fvm flutter` not bare `flutter`.
Dart commands: use `fvm dart`.
SDK constraint in pubspec.yaml: `^3.12.2` — do not change.
Civic issue reporting app. Drift for offline DB. Reports sync to REST API. No auth — anonymous only.
